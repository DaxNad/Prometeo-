# PROMETEO TASK ROUTING

AI routing rules:

architecture tasks:
→ logical validation
→ avoid direct coding

targeted coding tasks:
→ local patch
→ modify existing files

multi-file tasks:
→ preserve domain coherence
→ avoid duplication

planner tasks:
→ preserve ProductionEvent semantics

SMF tasks:
→ preserve Excel → domain mapping

refactor tasks:
→ do not modify implicit behaviour

test tasks:
→ cover domain regressions
