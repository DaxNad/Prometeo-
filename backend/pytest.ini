[pytest]
testpaths = tests
pythonpath = . ..
addopts = -m "not e2e"
markers =
    e2e: end-to-end tests that require a running server
    schema: schema guard checks for API responses
    dbschema: database schema guard checks for Postgres
filterwarnings =
    ignore::DeprecationWarning
